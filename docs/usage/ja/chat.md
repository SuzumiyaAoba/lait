# 会話セッションと対話モード（lait chat）

[ドキュメント目次に戻る](./README.md)

## `lait chat`（インタラクティブなマルチターン会話）

`lait chat` は会話履歴を保持したままターンを重ねられる REPL（対話モード）を起動します。応答は既定でストリーミング表示されます。

```sh
lait chat --model "モデル ID"
```

引数もプロンプトも与えずに `lait` を実行した場合も、標準入力が端末（TTY）であれば同じ REPL が起動します。標準入力がパイプで、かつ内容が空の場合は（スクリプトからの誤用を検知できるよう）従来通りエラーになります。

REPL 内のメタコマンド:

| コマンド | 説明 |
| --- | --- |
| `/exit` | REPL を終了する（標準入力の終端(Ctrl-D)でも終了します） |
| `/clear` | インメモリの会話履歴をクリアする（`--session` のファイルへの追記には影響しません） |
| `/model <name>` | このターン以降に使うモデルを切り替える |
| `/system <text>` | システムプロンプトを差し替える |

`--mcp`/`--subagent` を指定した場合も、そのターンは引き続きストリーミング表示されます。モデルがツールを呼び出すと、そのラウンドのテキスト（あれば）を表示したうえでツールを実行し、次のラウンドのストリームに進みます。`--session` を指定すると、REPL の各ターンも同じセッションファイルに記録されます。`--show-usage` を指定すると、ターンごとにそのセッション開始からの累積トークン使用量が表示されます。

## `--session`（会話の保存と再開）

`--session <NAME>` を指定すると、単発の `lait` 呼び出しでも会話履歴を引き継げます。

```sh
lait --model "モデル ID" --session demo "1+1は?"
lait --model "モデル ID" --session demo "それに2を足すと?"
```

- 保存先はプロジェクトローカルの `.lait/sessions/<NAME>.jsonl`（1行1メッセージの JSON Lines）です。
- リクエスト送信前に、そのセッションにこれまで記録されている全ターンを読み込み、システムプロンプトの直後・今回のプロンプトの直前に差し込みます。
- リクエストが成功した場合のみ、今回のユーザーメッセージとモデルの応答をセッションファイルに追記します（失敗したターンは記録されません）。
- `--stream` と併用できます（ストリーミング中に表示された内容全体が、その回のモデル応答として記録されます）。
- システムプロンプト（`--system`/`--system-file`/`default.system`）はセッションファイルには保存されません。毎回のコマンドラインから改めて渡してください。
- セッション名に使えるのは英数字・`_`・`-` のみです。

## `lait sessions`（セッションの一覧・確認・削除）

```sh
lait sessions list              # 保存されている全セッションとターン数を表示
lait sessions show demo         # demo セッションの全ターンを表示
lait sessions delete demo       # demo セッションを削除
```
