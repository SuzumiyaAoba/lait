/target/

# Local credentials and environment-specific configuration
.env
.env.*
!.env.example

# APM dependencies
apm_modules/

# Project-local runtime state: --session conversation logs (.lait/sessions/),
# --checkpoint/--resume run state (.lait/runs/), and the response disk cache
# (.lait/cache/, see --cache)
.lait/
