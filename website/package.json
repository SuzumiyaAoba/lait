{
  "name": "lait-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-docs": "node scripts/sync-docs.mjs",
    "sync-docs:check": "node scripts/sync-docs.mjs --check",
    "dev": "node scripts/sync-docs.mjs && blume dev",
    "build": "node scripts/sync-docs.mjs && blume check && blume validate && blume build",
    "preview": "blume preview",
    "types:check": "node scripts/sync-docs.mjs && blume check",
    "doctor": "blume doctor"
  },
  "dependencies": {
    "blume": "1.7.0"
  }
}
